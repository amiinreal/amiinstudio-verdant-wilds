project_id = "somali-airlines"

[functions.airline]
# The function reads the user from the Authorization header itself, so guests
# (reference + last name) can manage bookings without an account.
verify_jwt = false

[functions.stripe-webhook]
# Stripe signs its requests; there is no Supabase JWT.
verify_jwt = false
