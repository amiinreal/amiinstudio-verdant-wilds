<Window x:Class="Amiin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:views="clr-namespace:Amiin.Views"
        xmlns:vm="clr-namespace:Amiin.ViewModels"
        xmlns:conv="clr-namespace:Amiin.Converters"
        mc:Ignorable="d"
        Title="Amiin Studio" Width="1440" Height="900" MinWidth="1280" MinHeight="800"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="False"
        Background="{StaticResource BackgroundBrush}" Foreground="{StaticResource TextPrimaryBrush}"
        FontFamily="{StaticResource BodyFont}">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="40" ResizeBorderThickness="6" GlassFrameThickness="0" CornerRadius="0"/>
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <conv:TypeMatchConverter x:Key="TypeMatchConverter"/>
        <conv:InverseTypeMatchConverter x:Key="InverseTypeMatchConverter"/>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <DataTemplate DataType="{x:Type vm:LibraryViewModel}">
            <views:LibraryView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:GameDetailViewModel}">
            <views:GameDetailView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:LoginViewModel}">
            <views:LoginView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:DownloadsViewModel}">
            <views:DownloadsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <views:SettingsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:PlaceholderViewModel}">
            <views:PlaceholderView/>
        </DataTemplate>
    </Window.Resources>

    <Grid x:Name="RootGrid">

        <!-- ===================== Application shell (Library / GameDetail) ===================== -->
        <Grid Visibility="{Binding CurrentViewModel, Converter={StaticResource InverseTypeMatchConverter}, ConverterParameter=LoginViewModel}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <views:SidebarView Grid.Column="0" ActiveRoute="Library"/>

            <ContentControl Grid.Column="1" Content="{Binding CurrentViewModel}"/>
        </Grid>

        <!-- ===================== Sign-in overlay ===================== -->
        <ContentControl Content="{Binding CurrentViewModel}"
                         Visibility="{Binding CurrentViewModel, Converter={StaticResource TypeMatchConverter}, ConverterParameter=LoginViewModel}"/>

        <!-- ===================== Install / Update / Launch overlay ===================== -->
        <views:StatusView DataContext="{Binding Status}"
                           Visibility="{Binding DataContext.IsStatusVisible, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVis}}"/>

        <!-- ===================== Global window chrome buttons ===================== -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,6,10,0" shell:WindowChrome.IsHitTestVisibleInChrome="True">
            <Button x:Name="MinimizeButton" Style="{StaticResource WindowControlButtonStyle}" Click="Minimize_Click">
                <Path Data="{StaticResource IconMinus}" Style="{StaticResource LineIconStyle}" Width="12" Height="12"/>
            </Button>
            <Button x:Name="MaximizeButton" Style="{StaticResource WindowControlButtonStyle}" Click="Maximize_Click" Margin="2,0,0,0">
                <Path Data="{StaticResource IconSquare}" Style="{StaticResource LineIconStyle}" Width="11" Height="11"/>
            </Button>
            <Button x:Name="CloseButton" Style="{StaticResource CloseWindowControlButtonStyle}" Click="Close_Click" Margin="2,0,0,0">
                <Path Data="{StaticResource IconClose}" Style="{StaticResource LineIconStyle}" Width="12" Height="12"/>
            </Button>
        </StackPanel>

    </Grid>
</Window>
