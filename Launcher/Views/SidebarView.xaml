<UserControl x:Class="Amiin.Views.SidebarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" Width="248">
    <Border Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush1}" BorderThickness="0,0,1,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="76"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Logo chip -->
            <Border Grid.Row="0" VerticalAlignment="Center" Margin="20,0,0,0" HorizontalAlignment="Left"
                    Background="White" CornerRadius="10" Padding="10,6">
                <Image Source="pack://siteoforigin:,,,/Assets/amiin-logo.png" Height="22" Stretch="Uniform"/>
            </Border>

            <!-- Nav -->
            <StackPanel Grid.Row="1" Margin="12,16,12,0">
                <RadioButton x:Name="NavLibrary" GroupName="SidebarNav" Style="{StaticResource NavLinkStyle}"
                             Margin="0,0,0,4" IsChecked="True" Tag="Library">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="{StaticResource IconGrid}" Style="{StaticResource LineIconStyle}" Width="18" Height="18"/>
                        <TextBlock Text="Library" Margin="12,0,0,0" VerticalAlignment="Center" FontFamily="{StaticResource DisplayFont}" FontWeight="SemiBold" FontSize="14"/>
                    </StackPanel>
                </RadioButton>
                <RadioButton x:Name="NavStore" GroupName="SidebarNav" Style="{StaticResource NavLinkStyle}" Margin="0,0,0,4" Tag="Store">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="{StaticResource IconStore}" Style="{StaticResource LineIconStyle}" Width="18" Height="18"/>
                        <TextBlock Text="Store" Margin="12,0,0,0" VerticalAlignment="Center" FontFamily="{StaticResource DisplayFont}" FontWeight="SemiBold" FontSize="14"/>
                    </StackPanel>
                </RadioButton>
                <RadioButton x:Name="NavDownloads" GroupName="SidebarNav" Style="{StaticResource NavLinkStyle}" Margin="0,0,0,4" Tag="Downloads">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="{StaticResource IconDownloadTray}" Style="{StaticResource LineIconStyle}" Width="18" Height="18"/>
                        <TextBlock Text="Downloads" Margin="12,0,0,0" VerticalAlignment="Center" FontFamily="{StaticResource DisplayFont}" FontWeight="SemiBold" FontSize="14"/>
                    </StackPanel>
                </RadioButton>
                <RadioButton x:Name="NavFriends" GroupName="SidebarNav" Style="{StaticResource NavLinkStyle}" Margin="0,0,0,4" Tag="Friends">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="{StaticResource IconPeople}" Style="{StaticResource LineIconStyle}" Width="18" Height="18"/>
                        <TextBlock Text="Friends" Margin="12,0,0,0" VerticalAlignment="Center" FontFamily="{StaticResource DisplayFont}" FontWeight="SemiBold" FontSize="14"/>
                    </StackPanel>
                </RadioButton>
                <RadioButton x:Name="NavSettings" GroupName="SidebarNav" Style="{StaticResource NavLinkStyle}" Margin="0,0,0,4" Tag="Settings">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="{StaticResource IconGear}" Style="{StaticResource LineIconStyle}" Width="18" Height="18"/>
                        <TextBlock Text="Settings" Margin="12,0,0,0" VerticalAlignment="Center" FontFamily="{StaticResource DisplayFont}" FontWeight="SemiBold" FontSize="14"/>
                    </StackPanel>
                </RadioButton>
            </StackPanel>

            <!-- User row -->
            <Border Grid.Row="2" BorderBrush="{StaticResource BorderBrush1}" BorderThickness="0,1,0,0" Padding="16,14">
                <StackPanel Orientation="Horizontal">
                    <Border Width="34" Height="34" CornerRadius="17" Background="{StaticResource AvatarGradientBrush}">
                        <TextBlock x:Name="AvatarInitial" Text="A" Foreground="White" FontWeight="Bold" FontSize="14"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                        <TextBlock x:Name="UserNameText" Text="Explorer" FontSize="13" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                        <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                            <Ellipse Width="6" Height="6" Fill="{StaticResource GameGreenBrush}" VerticalAlignment="Center"/>
                            <TextBlock Text="Online" FontSize="11" Foreground="{StaticResource TextFaintBrush}" Margin="5,0,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>
